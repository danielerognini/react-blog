import { TabsItem } from '../types';
interface ActiveTagType {
    active: TabsItem['active'];
}
declare const ActiveTag: import("styled-components").StyledComponent<"span", import("styled-components").DefaultTheme, ActiveTagType, never>;
export default ActiveTag;
