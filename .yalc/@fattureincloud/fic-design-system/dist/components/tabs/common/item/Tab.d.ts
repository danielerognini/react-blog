import { TabsItem } from '../types';
interface TabTypes {
    active: TabsItem['active'];
}
declare const Tab: import("styled-components").StyledComponent<"span", import("styled-components").DefaultTheme, TabTypes, never>;
export default Tab;
